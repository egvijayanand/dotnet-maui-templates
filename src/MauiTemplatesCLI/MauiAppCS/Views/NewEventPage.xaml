<?xml version="1.0" encoding="UTF-8" ?>
<!--#if (Mvvm)-->
<!--#if (ImplicitNamespace)-->
<ContentPage
    x:Class="MauiApp._1.Views.NewEventPage"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    Padding="{OnPlatform iOS={StaticResource ApplePadding},
                         Default={StaticResource DefaultPadding}}"
    Shell.PresentationMode="ModalAnimated"
    Title="{__BIND__Heading}"
    x:DataType="NewEventViewModel">
<!--#elif (GlobalNamespace)-->
<ContentPage
    x:Class="MauiApp._1.Views.NewEventPage"
    xmlns="http://schemas.microsoft.com/dotnet/maui/global"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://schemas.microsoft.com/dotnet/maui/global/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    Padding="{OnPlatform iOS={StaticResource ApplePadding},
                         Default={StaticResource DefaultPadding}}"
    Shell.PresentationMode="ModalAnimated"
    Title="{__BIND__Heading}"
    x:DataType="NewEventViewModel"
    mc:Ignorable="d">
<!--#else-->
<ContentPage
    x:Class="MauiApp._1.Views.NewEventPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://schemas.microsoft.com/dotnet/2021/maui/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ctrl="clr-namespace:MauiApp._1.Controls"
    xmlns:local="clr-namespace:MauiApp._1"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:vm="clr-namespace:MauiApp._1.ViewModels"
    Padding="{OnPlatform iOS={StaticResource ApplePadding},
                         Default={StaticResource DefaultPadding}}"
    Shell.PresentationMode="ModalAnimated"
    Title="{__BIND__Heading}"
    x:DataType="vm:NewEventViewModel"
    mc:Ignorable="d">
<!--#endif-->
    <ContentPage.Content>
        <StackLayout>
            <Entry
                Text="{__BIND__Occasion.Name}"
                Placeholder="Event name"
                MaxLength="100" />
            <Entry
                Text="{__BIND__Occasion.Location}"
                Placeholder="Location"
                MaxLength="100" />
<!--#if (GlobalNamespace || ImplicitNamespace)-->
            <DateTimePicker
                Title="Start:"
                Date="{__BIND__Occasion.StartsAt.Date}"
                MinimumDate="{x:Static sys:DateTime.Now}"
                Time="{__BIND__Occasion.StartsAt.TimeOfDay}" />
            <DateTimePicker
                Title="End:"
                Date="{__BIND__Occasion.EndsAt.Date}"
                MinimumDate="{x:Static sys:DateTime.Now}"
                Time="{__BIND__Occasion.EndsAt.TimeOfDay}" />
<!--#else-->
            <ctrl:DateTimePicker
                Title="Start:"
                Date="{__BIND__Occasion.StartsAt.Date}"
                MinimumDate="{x:Static sys:DateTime.Now}"
                Time="{__BIND__Occasion.StartsAt.TimeOfDay}" />
            <ctrl:DateTimePicker
                Title="End:"
                Date="{__BIND__Occasion.EndsAt.Date}"
                MinimumDate="{x:Static sys:DateTime.Now}"
                Time="{__BIND__Occasion.EndsAt.TimeOfDay}" />
<!--#endif-->
            <Editor
                Text="{__BIND__Occasion.Notes}"
                HeightRequest="100"
                Placeholder="Notes"
                MaxLength="500" />
            <HorizontalStackLayout HorizontalOptions="Center">
                <Button
                    Command="{__BIND__SaveCommand}"
                    Style="{StaticResource PrimaryAction}"
                    Text="Save" />
                <Button
                    Command="{__BIND__CancelCommand}"
                    Style="{StaticResource Action}"
                    Text="Cancel" />
            </HorizontalStackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
<!--#else-->
<!--#if (ImplicitNamespace)-->
<ContentPage
    x:Class="MauiApp._1.Views.NewEventPage"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    Padding="{OnPlatform iOS={StaticResource ApplePadding},
                         Default={StaticResource DefaultPadding}}"
    Shell.PresentationMode="ModalAnimated"
    Title="New Event">
<!--#elif (GlobalNamespace)-->
<ContentPage
    x:Class="MauiApp._1.Views.NewEventPage"
    xmlns="http://schemas.microsoft.com/dotnet/maui/global"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://schemas.microsoft.com/dotnet/maui/global/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    Padding="{OnPlatform iOS={StaticResource ApplePadding},
                         Default={StaticResource DefaultPadding}}"
    Shell.PresentationMode="ModalAnimated"
    Title="New Event"
    mc:Ignorable="d">
<!--#else-->
<ContentPage
    x:Class="MauiApp._1.Views.NewEventPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://schemas.microsoft.com/dotnet/2021/maui/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ctrl="clr-namespace:MauiApp._1.Controls"
    xmlns:local="clr-namespace:MauiApp._1"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    Padding="{OnPlatform iOS={StaticResource ApplePadding},
                         Default={StaticResource DefaultPadding}}"
    Shell.PresentationMode="ModalAnimated"
    Title="New Event"
    mc:Ignorable="d">
<!--#endif-->
    <ContentPage.Content>
        <StackLayout>
            <Entry
                Text=""
                Placeholder="Event name"
                MaxLength="100" />
            <Entry
                Text=""
                Placeholder="Location"
                MaxLength="100" />
<!--#if (GlobalNamespace || ImplicitNamespace)-->
            <DateTimePicker
                x:Name="start"
                MinimumDate="{x:Static sys:DateTime.Now}"
                Title="Start:" />
            <DateTimePicker
                x:Name="end"
                MinimumDate="{x:Static sys:DateTime.Now}"
                Title="End:" />
<!--#else-->
            <ctrl:DateTimePicker
                x:Name="start"
                MinimumDate="{x:Static sys:DateTime.Now}"
                Title="Start:" />
            <ctrl:DateTimePicker
                x:Name="end"
                MinimumDate="{x:Static sys:DateTime.Now}"
                Title="End:" />
<!--#endif-->
            <Editor
                Text=""
                HeightRequest="100"
                Placeholder="Notes"
                MaxLength="500" />
            <HorizontalStackLayout HorizontalOptions="Center">
                <Button
                    Clicked="OnSave"
                    Style="{StaticResource PrimaryAction}"
                    Text="Save" />
                <Button
                    Clicked="OnCancel"
                    Style="{StaticResource Action}"
                    Text="Cancel" />
            </HorizontalStackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
<!--#endif-->
